# Python dependencies. Install with:  pip install -r requirements.txt
# Not pip-installable (build from source, see README Step 3): SZ3 + its pysz wrapper, SPERR (sperr3d).
numpy
torch
matplotlib
pandas
optuna
monai        # BasicUNet used by the NeurLZ baseline
jupyter
nbconvert
